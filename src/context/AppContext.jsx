/**
 * Global app state — cursorrules project structure.
 * Implementation: `app-context.js` + `AppProvider.jsx` + `hooks/useApp.js`.
 */
export { AppContext } from './app-context'
export { AppProvider } from './AppProvider'
